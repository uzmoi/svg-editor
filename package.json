{
  "private": true,
  "name": "svg-editor",
  "scripts": {
    "build": "spago bundle-app --to output/build.js && vite build",
    "dev:spago": "spago build -wx",
    "dev:vite": "> output/build.js && vite",
    "dev": "notios dev:spago dev:vite",
    "preview": "vite preview",
    "test": "spago test"
  },
  "devDependencies": {
    "notios": "^0.4.0",
    "purescript": "^0.15.2",
    "purty": "^7.0.0",
    "sass": "^1.53.0",
    "spago": "^0.20.9",
    "vite": "^2.9.11"
  }
}
